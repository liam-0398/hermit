Simple shell written in Common Lisp. Uses cl-readline for history support. Has quick shortcut !!! to drop to REPL.

Run with 
```
sbcl --load hermit.lisp
```

In file there is a line to uncomment to compile to standalone binary if you wish.

No pipes or > redirect currently. TBD.
